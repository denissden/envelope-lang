document.getElementById('envelope').addEventListener('keyup', convert, false)


function convert() {
    text, err = document.getElementById('envelope').value;
    if (err != null) {
        console.log(`Error! ${err}`)
    }
    document.getElementById('js').value = envelope(text);
}